{
  "name": "openvidu-integration-levels",
  "version": "1.0.0",
  "private": true,
  "description": "Three ways to integrate video conferencing into your app with OpenVidu",
  "scripts": {
    "install:all": "npm --prefix backend install && npm --prefix frontend install"
  }
}
